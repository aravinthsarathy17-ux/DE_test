version: 2
models:
  - name: stg_customer
    description: "staging model for customer records"
    columns:
      - name: customer_id
        tests:
         - not_null
         - unique
         